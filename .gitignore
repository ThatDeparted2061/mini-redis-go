*.exe
/bin/
data/
.idea/
.vscode/

# append-only persistence files (runtime state, not source)
*.aof

# graphify knowledge-graph output (deployed via gh-pages, not source)
graphify-out/

# local-only tooling config — kept on-machine, never pushed
CLAUDE.md
.serena/

# Grafana Cloud remote_write token — secret, never commit (see prometheus.yml)
deploy/prometheus/grafana_token